<%= form_for(@student_file) do |f| %>
  <% if @student_file.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@student_file.errors.count, "error") %> prohibited this student_file from being saved:</h2>

      <ul>
      <% @student_file.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :student_id %><br />
    <%= f.number_field :student_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
